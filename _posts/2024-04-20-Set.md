

```c
void union(int A[], int B[])
{
    int C[];

    while(a != NULL && b != NULL)
    {
        if (a > b)
            C.addLast(b)
            B.removeFirst()
        else if (a == b)
            C.addLast(b)
            A.removeFirst()
            B.removeFirst()
        else
        {
            C.addLast(a)
            A.removeFirst()
        }
    }
    while(!A.isEmpty())
        a <- A.get(1)
        C.addLast(a)
        A.removeFirst()
    while(!B.isEmpty())
        b <- B.get(1)
        C.addLast(b)
        B.removeFirst()
    return C
}
```



```c
void intersect(int A[], int B[])
{
    int C[];

    while(a != NULL && b != NULL)
    {
        if (a > b)
            B.removeFirst()
        else if (a == b)
            C.addLast(b)
            A.removeFirst()
            B.removeFirst()
        else
            A.removeFirst()
    }
    while(!A.isEmpty())
        A.removeFirst()
    while(!B.isEmpty())
        B.removeFirst()
    return C
}
```



```c
void intersect(int A[], int B[])
{
    int C[];

    while(a != NULL && b != NULL)
    {
        if (a > b)
            B.removeFirst()
        else if (a == b)
            A.removeFirst()
            B.removeFirst()
        else if (a < b)
            C.addLast(a)
            A.removeFirst()
    }
    while(!A.isEmpty())
        a <- A.get(1)
        C.addLast(a)
        A.removeFirst()
    while(!B.isEmpty())
        B.removeFirst()
    return C
}
```



```c
Alg member(int A[], int e)
{
    if (!A)
        return False
    p <- A
    while(1)
        a <- p.elem
        if (a < e)
            if (p.next == NULL)
                return False
            else
                p <- p.next
        else if (a > e)
            return False
        else (a == e)
            return Ture
}
```



```c
Alg Subset(int A[], int B[])
{
    if (!A)
        return Ture
    while (1)
        if(B.member(p.elem))
            if (p->next == NULL)
                return True
            else
                p <- p.next
        else
            return False
}
```





## Recursive

```c
Alg Union(int A[], int B[])
{
    while (A == NULL && B == NULL)
        return NULL
    p <- getnode()
    if while (A == NULL)
        p.elem <- B.elem
        Union(A, B.next)
    else if while (B == NULL)
        p.elem <- A.elem
        Union(A.next, B)
    else
    
}
```



---



## List of array

```c
void add(r, e)
{
    if (n = N)
        fullListException()
    if (r < 0 || r > n)
        invaildRandExceptoin()
    for (int i = n - 1; i >= r; i++)
       V[i] = V[i + 1]
    V[r] = e
    n += 1    
}
```

```c
void remove(r)
{
    for (int i = r + 1; i < n; i++)
        V[i - 1] = V[i]
    n -= 1
}
```
